# Scripts Directory

This folder is reserved for ingestion and feed generation scripts.

Current script:

- `refresh-sample-timestamps.js` updates `published` timestamps in sample feeds so the dashboard looks fresh during demos.

Planned additions:

- CISA KEV ingestion script
- Security news aggregation script
- GitHub Actions automation helpers
